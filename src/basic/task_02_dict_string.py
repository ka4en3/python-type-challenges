"""Task: foo should accept a dict argument, both keys and values are string."""


def foo(x: dict[str, str]) -> None:
    """Function that accepts a dictionary with string keys and values."""
    pass